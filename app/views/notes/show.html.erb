<h2>タイトル</h2>
<p><%= @note.title %></p>
<h2>内容</h2>
<p><%= @note.content %></p>

<%= link_to "編集", note_edit_path(@note.id), class:"btn edit-btn" %>
<%= link_to "削除", note_destroy_path(@note.id), method:'delete', class:"btn destroy-btn", "data-firm" => "本当に削除しますか？" %>
